import {Redirect} from 'react-router-dom'
import Cookies from 'js-cookie'
import Header from '../Header'

const Home = () => (
  <div>
    <Header />
    <h1>Home Route</h1>
  </div>
)

export default Home
